#!/usr/bin/make -f

#export DH_VERBOSE = 1
export PYBUILD_NAME = mtls

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_install:
	python3.7 setup.py install --prefix=/usr/

override_dh_auto_test:
	$(exit 0)
